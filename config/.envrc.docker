# Ookkee Docker Environment

# Database Configuration (Docker service names)
DB_HOST=db
DB_PORT=5432
DB_NAME=ookkee
DB_USER=postgres
DB_PASSWORD=postgres

# Server Configuration
SERVER_PORT=8080
UPLOADS_DIR=/app/uploads

# Frontend Configuration
VITE_API_URL=http://localhost:8080
